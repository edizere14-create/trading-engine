# Core
aiohttp>=3.9,<4
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.0,<3
redis>=5.0,<7
requests>=2.31,<3
python-dotenv>=1.0,<2

# Optional: EIP-712 intent signing (Arbitrum layer)
eth-account>=0.11,<1

# Testing (not needed in production)
# pytest>=8.0
# pytest-asyncio>=0.23
